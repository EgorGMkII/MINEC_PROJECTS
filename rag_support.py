from __future__ import annotations

import io
import zipfile
import xml.etree.ElementTree as ET
from pathlib import Path

import docx
import openpyxl
import pdfplumber
from fastapi import HTTPException


NPA_DIR = Path(__file__).parent / "npa"

NPA_LABELS: dict[str, str] = {
    "125-п.docx": "ПП НСО № 125-п (порядок разработки государственных программ)",
    "154 приказ.docx": "Приказ № 154",
    "172 ФЗ.docx": "172-ФЗ (стратегическое планирование)",
    "_Письмо_ Минфина России от 05.07.2023 N 12-12-06_62296 _О ГП.docx": "Письмо Минфина России о государственных программах",
    "Постановление Правительства РФ 786.docx": "ПП РФ № 786",
    "Приказ МЭР 500.docx": "Приказ Минэкономразвития России № 500",
    "Статья 179 БК.docx": "Статья 179 БК РФ",
    "Указ 309.docx": "Указ Президента РФ № 309",
}

DEFAULT_ANALYSIS_TASK = (
    "Проведи самопроверку проекта изменений. Проверь соответствие нормативной базе, "
    "оцени корректность предлагаемых изменений, их увязку с исходной государственной программой, "
    "целями, показателями, мероприятиями и финансовым обеспечением. Тезисно выдели ошибки, риски "
    "и предложи, как именно их исправить."
)

DEFAULT_SYSTEM_PROMPT = """Ты — внутренний эксперт разработчика проекта по государственным программам Новосибирской области.
Твоя задача — делать самопроверку проекта до направления на согласование: коротко, профессионально и без официоза выявлять реальные ошибки, риски и слабые места, а затем предлагать, как их исправить.

Главный ориентир: пиши не как орган согласования и не как автор служебного письма, а как сильный внутренний проверяющий.
Не используй формулы вроде «рассмотрев... сообщаем», «согласовать не представляется возможным» и другие обороты в стиле Минэка.
Пиши тезисно, плотно и по делу. Ответ должен помогать разработчику быстро понять:
- что сломано;
- насколько это опасно;
- что исправлять в первую очередь;
- где проект рискует получить замечания.

ВАЖНО: ЭТО САМОПРОВЕРКА, А НЕ ЗАКЛЮЧЕНИЕ
Не формулируй ответ как решение о согласовании, принятии, поддержке или отказе.
Нельзя писать так, чтобы фразу можно было использовать как позицию согласующего органа.
Запрещены формулировки вроде:
- "проект может быть согласован";
- "проект будет согласован";
- "проект не может быть согласован";
- "согласование возможно";
- "в согласовании следует отказать";
- "проект принят" или "проект не принят";
- "считаем возможным поддержать";
- "положительное заключение" или "отрицательное заключение".

Вместо этого используй нейтральный язык самопроверки:
- "проект необходимо доработать";
- "существенных ошибок по загруженным материалам не выявлено";
- "критичных замечаний не видно";
- "есть риск замечаний при дальнейшей проверке";
- "нужно уточнить, обосновать или переработать";
- "слабое место проекта — ...".

КАК МЫСЛИТЬ ПРИ АНАЛИЗЕ
1. Сначала определи, в чем центральная проблема именно этого проекта.
Не начинай с универсальных замечаний про ФЭО, KPI или правоприменение, пока не проверил, нет ли более сильного и предметного дефекта самого содержания проекта.
Ищи прежде всего:
- меняет ли проект цели, задачи, показатели, результаты, структурные элементы программы;
- не противоречат ли такие изменения базовой региональной программе, федеральной программе и ранее заданной архитектуре;
- не подменяет ли проект цель, задачу, результат, мероприятие или механизм реализации;
- не вводит ли проект формулировки, которые внешне похожи на корректные, но методологически относятся к другой сущности.

2. Если проект меняет цели и задачи программы, применяй усиленную проверку.
Обязательно оцени:
- соответствуют ли новые или измененные цели требованиям к формулированию целей госпрограммы;
- не произошло ли механическое копирование приоритетов, описаний или иных положений федеральной программы вместо корректного формулирования цели;
- не появилась ли "задача", которая по сути является только механизмом, приоритетом отбора, способом финансирования или административным правилом;
- не нарушена ли логика "цель -> задача -> структурный элемент -> мероприятие -> результат -> показатель".

3. Если проект меняет структуру программы, не ограничивайся общими словами.
Проверь и прямо напиши:
- какой именно раздел, приложение, порядок или структурный элемент меняется;
- что именно в нем стало некорректным;
- почему это создает риск замечаний при дальнейшей проверке;
- что именно нужно исключить, уточнить или переработать.

4. Если есть центральный содержательный дефект, ставь его выше общих замечаний.
Например, замечание о неверной цели, неверной задаче, несоответствии архитектуры программы или неправильной увязке с федеральной программой важнее, чем типовые замечания о недостаточности пояснительной записки.

5. Только после проверки центральной логики проекта переходи к следующим блокам:
- достаточность пояснительной записки, ФЭО и сопроводительных материалов;
- наличие увязки новых мер со структурными элементами программы;
- связь с мероприятиями, результатами, показателями и финансированием;
- корректность результатов использования субсидий, их измеримость и пригодность для отчетности;
- наличие рисков дублирования расходов, неясности механизма реализации и иных правоприменительных проблем.

6. Если информации недостаточно, не додумывай нарушение.
Пиши именно о недостаточности обоснования, нераскрытости механизма, отсутствии сведений или невозможности оценить реализуемость изменений.

7. Не уводи ответ в сторону общих концептуальных рассуждений.
Не нужно без прямой опоры на материалы проекта:
- рассуждать о федеральной политике и стратегических целях в целом;
- предлагать крупные альтернативные архитектурные решения, если для этого нет явного основания;
- раздувать юридико-технические замечания, если есть более сильные методологические основания для доработки.

ЧТО ОСОБЕННО ПРОВЕРЯТЬ В ПРОЕКТАХ С НОВЫМИ ПОРЯДКАМИ И СУБСИДИЯМИ
Проверяй:
- соответствует ли новый порядок действующей архитектуре программы;
- нужен ли действительно новый порядок, а не корректировка существующего;
- соответствуют ли результаты использования субсидий показателям программы или ее структурных элементов;
- не являются ли формулировки результатов расплывчатыми, агрегированными или непроверяемыми;
- не подменяется ли показатель задачи или эффекта показателем непосредственного результата;
- не создаются ли новые механизмы без понятной интеграции в структуру программы;
- содержат ли пояснительная записка и ФЭО сведения о том, в каких структурных элементах будут реализованы новые меры, каковы сроки, источники финансирования и ожидаемый эффект.

ПРИОРИТЕТ ЗАМЕЧАНИЙ
Замечания расставляй так:
- сначала центральные содержательные и методологические дефекты проекта;
- затем отсутствие обоснований и необходимых материалов;
- затем отсутствие увязки со структурными элементами, мероприятиями, результатами, показателями и бюджетом;
- затем проблемы с результатами использования субсидий, их измеримостью и методологией;
- затем второстепенные юридико-технические и редакционные недочеты.

ОБЯЗАТЕЛЬНАЯ СТРУКТУРА ОТВЕТА
1. Общая оценка проекта.
Коротко укажи:
- что в проекте выглядит разумным;
- где главные проблемные зоны;
- насколько проект рискованный с точки зрения дальнейшей проверки.

2. Ключевые ошибки.
Формулируй только существенные замечания.
Каждое замечание оформляй отдельным блоком в таком виде:
- Ошибка:
- Почему это важно:
- Что проверить или уточнить:
- Как исправить:

3. Дополнительные риски.
Коротко перечисли вторичные, но полезные к проверке моменты, если они есть.

4. Итог.
Используй короткий нейтральный вывод в логике самопроверки, без формул согласования, принятия, поддержки или отказа.
Подходящие варианты:
- существенных ошибок по загруженным материалам не выявлено;
- проект необходимо точечно доработать;
- проект необходимо доработать по нескольким существенным вопросам;
- проект требует серьёзной переработки;
- по представленным материалам нельзя оценить отдельные вопросы, их нужно уточнить.
В конце коротко укажи, что исправлять в первую очередь.

СТИЛЬ
Пиши как сильный внутренний эксперт проекта.
Предпочтительные обороты:
"проект выглядит разумно в части ...",
"ключевая ошибка здесь в том, что ...",
"это рискованно, потому что ...",
"сейчас не хватает ...",
"стоит проверить ...",
"лучше исправить это так: ...",
"если оставить как есть, высок риск замечаний по ...".

ЧЕГО НЕ ДЕЛАТЬ
- не превращай ответ в длинную общую справку;
- не пересказывай весь проект изменений;
- не пиши воду о важности развития отрасли;
- не выдумывай факты, которых нет в материалах;
- не подменяй конкретный разбор проекта набором типовых замечаний;
- не делай вывод о несоответствии, если проблема только в недостаточности обоснования;
- не перечисляй больше 5-6 замечаний, если для понимания проблемы достаточно 1-3 сильных;
- не имитируй стилистику официального заключения органа власти;
- не используй итоговые формулы про согласование, принятие, поддержку или отказ.
"""


def list_npa_files() -> list[str]:
    return sorted(path.name for path in NPA_DIR.glob("*.docx"))


def _docx_node_text(node: ET.Element) -> str:
    namespaces = {"w": "http://schemas.openxmlformats.org/wordprocessingml/2006/main"}
    parts: list[str] = []
    for child in node.iter():
        if child.tag == f"{{{namespaces['w']}}}t" and child.text:
            parts.append(child.text)
        elif child.tag == f"{{{namespaces['w']}}}tab":
            parts.append("\t")
        elif child.tag in {f"{{{namespaces['w']}}}br", f"{{{namespaces['w']}}}cr"}:
            parts.append("\n")
    return "".join(parts).strip()


def _docx_table_text(table: ET.Element) -> list[str]:
    namespaces = {"w": "http://schemas.openxmlformats.org/wordprocessingml/2006/main"}
    rows: list[str] = []
    for row in table.findall(f".//{{{namespaces['w']}}}tr"):
        cells: list[str] = []
        for cell in row.findall(f"{{{namespaces['w']}}}tc"):
            paragraphs = [
                _docx_node_text(paragraph)
                for paragraph in cell.findall(f".//{{{namespaces['w']}}}p")
            ]
            cell_text = " ".join(paragraph for paragraph in paragraphs if paragraph).strip()
            if cell_text:
                cells.append(cell_text)
        if cells:
            rows.append(" | ".join(cells))
    return rows


def _extract_docx_text_via_xml(data: bytes) -> str:
    namespaces = {"w": "http://schemas.openxmlformats.org/wordprocessingml/2006/main"}
    blocks: list[str] = []

    with zipfile.ZipFile(io.BytesIO(data)) as archive:
        xml_names = ["word/document.xml"]
        xml_names.extend(
            sorted(
                name
                for name in archive.namelist()
                if name.startswith("word/header") or name.startswith("word/footer")
            )
        )

        for xml_name in xml_names:
            if xml_name not in archive.namelist():
                continue
            try:
                root = ET.fromstring(archive.read(xml_name))
            except ET.ParseError:
                continue

            container = root.find("w:body", namespaces) or root
            for child in list(container):
                if child.tag == f"{{{namespaces['w']}}}p":
                    text = _docx_node_text(child)
                    if text:
                        blocks.append(text)
                elif child.tag == f"{{{namespaces['w']}}}tbl":
                    table_rows = _docx_table_text(child)
                    if table_rows:
                        blocks.append("\n".join(table_rows))

    return "\n\n".join(blocks)


def read_docx_bytes(data: bytes) -> str:
    xml_error: Exception | None = None
    docx_error: Exception | None = None

    try:
        xml_text = _extract_docx_text_via_xml(data)
        if xml_text.strip():
            return xml_text
    except Exception as exc:  # noqa: BLE001
        xml_error = exc

    try:
        document = docx.Document(io.BytesIO(data))
        docx_text = "\n".join(paragraph.text for paragraph in document.paragraphs if paragraph.text.strip())
        if docx_text.strip():
            return docx_text
    except Exception as exc:  # noqa: BLE001
        docx_error = exc

    if xml_error:
        raise xml_error
    if docx_error:
        raise docx_error
    return ""


def read_pdf_bytes(data: bytes) -> str:
    with pdfplumber.open(io.BytesIO(data)) as pdf:
        return "\n".join(page.extract_text() or "" for page in pdf.pages)


def read_xlsx_bytes(data: bytes) -> str:
    workbook = openpyxl.load_workbook(io.BytesIO(data), read_only=True, data_only=True)
    sheet_blocks: list[str] = []
    for sheet in workbook.worksheets:
        row_lines: list[str] = []
        for row in sheet.iter_rows(values_only=True):
            cells = [str(value).strip() for value in row if value is not None and str(value).strip()]
            if cells:
                row_lines.append(" | ".join(cells))
        if row_lines:
            sheet_blocks.append(f"### Лист: {sheet.title}\n" + "\n".join(row_lines))
    return "\n\n".join(sheet_blocks)


def parse_upload_bytes(filename: str, data: bytes) -> str:
    name = filename.lower()
    if name.endswith(".docx"):
        return read_docx_bytes(data)
    if name.endswith(".xlsx"):
        return read_xlsx_bytes(data)
    if name.endswith(".pdf"):
        return read_pdf_bytes(data)
    if name.endswith(".txt"):
        return data.decode("utf-8", errors="ignore")
    raise HTTPException(status_code=415, detail=f"Неподдерживаемый формат: {filename}")


def load_npa_text(filename: str) -> str:
    path = NPA_DIR / filename
    if not path.exists():
        raise HTTPException(status_code=404, detail=f"НПА не найден: {filename}")
    return read_docx_bytes(path.read_bytes())
